services:
  - type: web
    name: glow-bela
    env: node
    plan: free
    buildCommand: rm -rf node_modules/.vite dist && npm install && npm run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
